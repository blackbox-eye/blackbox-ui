--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@ -25,10 +25,21 @@
 name: CI & Deploy (Secure)
 
 on:
   push:
     branches: [ main ]
   workflow_dispatch:
 
+# Prevent concurrent deployments to production
+concurrency:
+  group: production-deployment
+  cancel-in-progress: false  # Don't cancel ongoing deployments
+
 # Explicit permissions for GITHUB_TOKEN (security best practice)
 permissions:
   contents: read
@@ -45,7 +56,7 @@
       contents: read
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
 
       - name: Verify critical files exist
         run: |
@@ -117,6 +128,7 @@
           echo "🔐 Connecting to FTP server with TLS encryption..."
           echo "   Host: $FTP_HOST"
           echo "   Path: $FTP_REMOTE_PATH"
           
           # Execute secure FTP operation
@@ -134,23 +146,35 @@
             
             # Connect and authenticate
             open -u $FTP_USERNAME,$FTP_PASSWORD $FTP_HOST;
             
             # Navigate to target directory
             cd $FTP_REMOTE_PATH || exit 1;
             
             # Check if index.html exists
             echo '🔍 Checking for index.html...';
             ls -la index.html 2>/dev/null && echo '   ✓ index.html found' || echo '   ℹ️  index.html not present';
             
             # Delete index.html (ignore error if not exists)
             rm -f index.html;
             
+            # Wait for server to process deletion
+            sleep 2;
+            
             # Verify deletion
             echo '✅ Deletion completed';
-            ls -la index.html 2>/dev/null && echo '⚠️  WARNING: index.html still exists!' || echo '   ✓ index.html successfully removed';
+            
+            # Explicitly fail if file still exists
+            if ls -la index.html 2>/dev/null; then
+              echo '❌ ERROR: index.html still exists after deletion!' >&2;
+              echo '   This indicates a server permission or caching issue' >&2;
+              exit 1;
+            else
+              echo '   ✓ index.html successfully removed';
+            fi
             
             # Close connection
             bye
           " 2>&1 | grep -v "^put: " | grep -v "^get: " || {
             EXIT_CODE=$?
             if [ $EXIT_CODE -ne 0 ]; then
               echo "❌ FTP operation failed with exit code $EXIT_CODE" >&2
@@ -184,7 +208,7 @@
       contents: read
     steps:
       - name: Checkout repository
-        uses: actions/checkout@v4
+        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
 
       - name: Prepare deployment
         run: |
@@ -199,7 +223,7 @@
           echo "✅ Deployment preparation complete"
 
       - name: Deploy via FTPS
-        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
+        uses: SamKirkland/FTP-Deploy-Action@65f5e8c8db370a98c3335cf0db0ff5a537c9fb52  # v4.3.5
         with:
           server: ${{ secrets.FTP_HOST }}
           username: ${{ secrets.FTP_USERNAME }}
@@ -357,12 +381,20 @@
           
           if [ "$HTTP_CODE" = "404" ]; then
             echo "✅ index.html correctly returns 404 (deleted successfully)"
           elif [ "$HTTP_CODE" = "403" ]; then
             echo "✅ index.html returns 403 (access denied - acceptable)"
           elif [ "$HTTP_CODE" = "301" ] || [ "$HTTP_CODE" = "302" ]; then
             echo "✅ index.html redirects (DirectoryIndex working)"
           else
             echo "⚠️  index.html returned HTTP $HTTP_CODE"
             echo "    This may require manual verification"
+            
+            # If index.html returns 200, this is a problem
+            if [ "$HTTP_CODE" = "200" ]; then
+              echo "❌ ERROR: index.html is still being served!" >&2
+              echo "   DirectoryIndex may not be working correctly" >&2
+              exit 1
+            fi
           fi
 
       - name: Test 6 - Verify index.php is served at root
